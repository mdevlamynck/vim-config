" CONTROL
" Change leader key
let mapleader = "_"

" Configure the behavior of backspaces
set backspace=indent,eol,start

" Enable the usage of the mouse
set mouse=a

" When replacing, don't delete the text to replace, show a end point instead
set cpoptions+=$
